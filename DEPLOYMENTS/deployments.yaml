apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-deployment
  labels:
    app: hello-world-app
spec: 
  replicas: 3
  selector:
    matchLabels:
      app: hello-world-app
  template:
    metadata:
      labels:
        app: hello-world-app
    spec:
      containers:
        - name: hello-world-container
          image: karthequian/helloworld
          ports:
            - containerPort: 80